trigger:
  - main

pool:
  vmImage: "ubuntu-latest"

steps:
  - script: |
      sudo apt-get update && sudo apt-get install -y shellcheck
      make lint
    displayName: "Run ShellCheck via Makefile"
